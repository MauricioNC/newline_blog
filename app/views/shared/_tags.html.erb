<% if tags.present? %>
  <% tags.each do |t| %>
    <%= link_to t.tag, posts_by_tag_path(t.tag), class: "border-2 border-black rounded-lg p-1 bg-gv-smalt-blue text-center xs:px-2 xs:text-sm lg:px-4 shadow-hard" %>
  <% end %>
<% end %>
