<% if tags.present? %>
  <% tags.each do |t| %>
    <%= link_to t.tag, posts_by_tag_path(t.tag), class: "rounded-lg p-1 text-gv-saffron bg-gv-baltic-sea shadow-2xl text-center xs:px-2 xs:text-sm lg:px-4" %>
  <% end %>
<% end %>
