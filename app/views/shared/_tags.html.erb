<div class="banner_tags flex justify-center items-center gap-2.5 shrink-0 flex-wrap mt-11">
  <% tags.each do |tag| %>
    <%= button_to tag.tag, "", class: "border-2 border-black rounded-lg p-1" %>
  <% end %>
</div>