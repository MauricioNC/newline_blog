<header class="col-span-12 flex flex-row justify-between items-center mt-3 z-10">
  <%= link_to "Logo", root_path %>
  <% if current_user %>
    <div class="burger-menu w-9 h-9 cursor-pointer relative overflow-hidden" data-controller="menu" data-menu-target="menu" data-action="click->menu#toggleMenu">
      <div class="h-full relative">
        <span class="top-line burguer-menu-lines"></span>
        <span class="mid-line burguer-menu-lines"></span>
        <span class="bottom-line burguer-menu-lines"></span>
      </div>
      <%= render partial: 'shared/drop_down' %>
    </div>
  <% else %>
    <%= link_to  "Login", sessions_login_path %>
  <% end %>
</header>
