<header class="top-bar px-3 lg:px-40 flex flex-row justify-between items-center z-10 mb-10 py-2 border-b-2 border-transparent" data-controller="menu">
  <%= link_to "Logo", root_path, class: 'text-2xl font-semibold' %>
  <% if current_user %>
    <%= render partial: 'shared/navbar' %>
  <% else %>
    <%= link_to "Login", login_path, class: 'px-4 py-2 bg-gv-pistachio rounded-lg border-2 border-black shadow-hard font-bold text-gv-dark-jungle' %>
  <% end %>
</header>
