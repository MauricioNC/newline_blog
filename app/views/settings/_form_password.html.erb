<%= form_with(model: user, class: 'col-span-12 mt-6') do |f| %>
  <div class="flex flex-col items-start">
    <%= f.label :password, "Password" %>
    <%= f.password_field :password, class: "w-full rounded" %>
  </div>
  <%= f.submit %>
<% end %>
