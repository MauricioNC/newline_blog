<%= render 'post', post: @post, like: @like %>

<div class="col-span-12 mt-8">
  <h2>Mas contenido como este</h2>
  <%= render "shared/cards", posts: @posts %>
</div>
  
<%= render "shared/footer" %>
